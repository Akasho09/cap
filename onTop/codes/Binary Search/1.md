## 
1. Insert Position : 
    - after smallest => 
        - int e = nums.size();
        -   if(target>nums[m]) s=m+1;
            else e=m;

2. First and Last position 
    - =>
    if (nums[mid] == target) {
                ans = mid; // store ans , low will not be answer.
                if (findFirst) high = mid - 1; // move left
                else low = mid + 1;  // move right
    }

