## 🔹 What is a Subnet?
- A subnet divides a large network into smaller logical networks.
- 👉 Instead of one huge network, you get multiple smaller networks, each called a subnet.

### 🔹 Subnet Mask Explained
- A subnet mask defines:
    - Which part is network
    - Which part is host
- IP Address:   192.168.1.10
- Subnet Mask:  255.255.255.0
- This means:
    - Network part → 192.168.1
    - Host part → .10

 ## Load Balancer?
-  A Load Balancer is a network device or software that distributes incoming network traffic across multiple servers to ensure:
✅ High availability
✅ Better performance
✅ No server overload

### 🔹 Types of Load Balancers
1. 1️⃣ Hardware Load Balancer
- Physical device
- Very fast but expensive
- Used in large data centers

2. 2️⃣ Software Load Balancer
- Software-based
- Runs on normal servers
- Examples:
    - Nginx
    - HAProxy
    - Apache

3. 3️⃣ Cloud Load Balancer
- Provided by cloud platforms
- Auto-scalable
- Examples:
    - AWS ELB / ALB
    - Google Cloud Load Balancer
    - Azure Load Balancer

### Proxy Servers :
- Exmaple :
    - 🏫 Scenario: College Internet Access
What happens?
- Student requests youtube.com
- Request goes to proxy server
- Proxy checks:
    - Is the site allowed?
    - Is it already cached?
- If allowed:
    - Proxy forwards request to YouTube
    - Gets response
    - Sends it back to student

## NAT (Network Address Translation) 
- is a technique used by routers to translate private IP addresses into a public IP address (and vice versa) when devices access the Internet.

### 🔹 Why NAT is used
1. IPv4 Address Conservation
- Multiple devices share one public IP
- Solves IPv4 exhaustion

2. Security
- Internal (private) IPs are hidden
- External users cannot directly access internal devices

3. Cost Efficient
- No need to buy multiple public IPs🔹 Why NAT is used

### 🔹 How NAT Works (Simple)
- Internal device: 192.168.1.10
- Router public IP: 203.0.113.5
- Router translates:
> 192.168.1.10 → 203.0.113.5

### 🔹 Types of NAT (Bonus Points)
- Static NAT – One private ↔ one public
- Dynamic NAT – Pool of public IPs
- PAT (Port Address Translation) – Many private → one public (most common)

## Classless Inter Domain Routing (CIDR)
CIDR(Classless Inter Domain Routing) is a method of IP address allocation and routing that allows more efficient use of IP addresses. Unlike traditional class-based addressing, CIDR allocates IP addresses based on a network prefix rather than a fixed class (A, B, or C).
- CIDR Notation: 
> a.b.c.d/n
- n = number of bits in the network prefix.
- Example: 192.168.1.0/24 first 24 bits are the network, and the remaining 8 bits are the host ID

### Why CIDR?
- Classful addressing wastes IP addresses:

- Problem: Organizations often need a number of hosts that do not match class sizes, leading to wastage.
- Solution: CIDR allows flexible block allocation matching exact requirements.

### Rules for Forming CIDR Blocks
- All IPs must be contiguous.
- Block size must be a power of 2 (2ⁿ) simplifies network division.
- First IP of block divisible by block size least significant bits of host ID should be 0.

### Example: Check whether 100.1.2.32 to 100.1.2.47 is a valid IP address block or not?
- All the IP addresses are contiguous.
- Total number of IP addresses in the Block = 16 = 24.
- 1st IP address: 100.1.2.00100000 Since, Host Id will contains last 4 bits and all the least significant 4 bits are zero. Hence, first IP address is evenly divisible by the size of the block.
- All three rules are followed by this Block. Hence, it is a valid IP address block.

### Advantages of CIDR
- Efficient IP usage: Minimizes IPv4 address wastage.
- Flexible allocation: Supports networks of any size.
- Improved routing: Aggregates addresses for faster, simpler routing.
- Lower administrative overhead: Easier IP and network management.

